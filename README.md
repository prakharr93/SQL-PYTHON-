# SQL-PYTHON-
A hands-on data analysis project using SQL and Python where I built tables, wrote advanced SQL queries, and analyzed the extracted data in Python. The project covers window functions, CTEs, aggregations, and joins, followed by EDA and visual storytelling using Pandas and Matplotlib.
